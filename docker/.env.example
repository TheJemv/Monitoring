# Copy this file to .env next to docker-compose.yml and fill in your values.

# Cloudflare Tunnel token (Zero Trust dashboard → Networks → Tunnels → your
# tunnel → "Install and run a connector"). Leave empty if you're not using
# cloudflared to expose the stack — just remove that service from
# docker-compose.yml in that case.
CLOUDFLARE_TUNNEL=
